data/**
models/**
!data/**/
!models/**/
!data/**/.gitkeep
!models/**/.gitkeep
!data/**/*.dvc
.env
__pycache__/
*.pyc
.venv*/
mlruns/
*.csv
*png
*.parquet
*.log
lightning_logs*/
.ipynb_checkpoints/
*.ckpt
Untitled*
mlflow.db

# keep dashboard result CSVs
!reports/*.csv

# Runtime caches written by the live forecast path. Regenerated on demand;
# committing them would churn the history and ship stale forecasts.
.cache/
reports/live_forecast.parquet
reports/live_forecast_meta.json

# Cached eval APE arrays (63 MB). Provenance for the metrics JSONs, but
# derived and regenerable -- kept on disk, out of the clone.
*.npz
